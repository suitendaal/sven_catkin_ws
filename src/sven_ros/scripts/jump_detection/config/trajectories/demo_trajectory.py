#!/usr/bin/python3

from datalib import *
import math

via_points = PoseDataSet([
	PoseDataPoint(0, [0.30, 0.54, 0.2, math.pi, 0, math.pi/2]),
	PoseDataPoint(3, [0.30, 0.54, 0.2, math.pi, 0, math.pi/2]),
	PoseDataPoint(6, [0.30, 0.54, 0.1, math.pi, 0, math.pi/2]),
	PoseDataPoint(9, [0.30, 0.54, 0.1, math.pi, 0, math.pi/2])
])
